export const API_BASE_URLS = {
  COMPANY: "https://jsonplaceholder.typicode.com",
  BACKEND: "https://jsonplaceholder.typicode.com",
};

export const DEFAULT_AUTH_TOKEN_HEADER_NAME = "Authorization";
export const DEFAULT_REQUEST_TIMEOUT = 25000;

export const INTERCEPTOR_TYPES = {
  REQUEST: "request",
  RESPONSE: "response",
};
